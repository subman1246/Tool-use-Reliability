[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tur"
version = "0.1.0"
description = "Invocation-level tool-use reliability for LLM agents"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
